#!/usr/bin/env node
// Committed launcher test/contract stub (fixtures/npm).
// Echoes argv to stdout and exits 0. Never shipped: real platform binaries
// are produced by `deno compile` and packaged under dist/.
"use strict";
process.stdout.write("stub-ok " + process.argv.slice(2).join(" ") + "\n");
process.exit(0);
